name: Keep Render Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: curl -s https://plataforma-de-cursos-eeow.onrender.com/health > /dev/null